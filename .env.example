# The URL of the Update Night API server.
# Defaults to https://server.updatenight.com if not set.
# Override to point at a local server during development.
# Defaults to https://server.updatenight.com if not set.
UPDATENIGHT_API_URL=https://server.updatenight.com
