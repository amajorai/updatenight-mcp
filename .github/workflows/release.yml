name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04-arm
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        run: rustup toolchain install stable --target ${{ matrix.target }}

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ matrix.target }}-cargo-

      - name: Build
        run: rustup run stable cargo build --release --target ${{ matrix.target }}

      - name: Package (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          cp target/${{ matrix.target }}/release/updatenight-mcp updatenight-mcp
          tar czf updatenight-mcp-${{ matrix.target }}.tar.gz updatenight-mcp
          rm updatenight-mcp

      - name: Package (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          Copy-Item "target\${{ matrix.target }}\release\updatenight-mcp.exe" "updatenight-mcp.exe"
          Compress-Archive -Path "updatenight-mcp.exe" -DestinationPath "updatenight-mcp-${{ matrix.target }}.zip"
          Remove-Item "updatenight-mcp.exe"

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            updatenight-mcp-${{ matrix.target }}.tar.gz
            updatenight-mcp-${{ matrix.target }}.zip
